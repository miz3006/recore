import { StubScreen } from '@/components/stub-screen';

/**
 * /paywall stub (CLAUDE.md §2): RevenueCat wires in later — no billing here.
 */
export default function Paywall() {
  return <StubScreen title="Recore Pro" note="Subscription (RevenueCat) lands here — stub only." />;
}

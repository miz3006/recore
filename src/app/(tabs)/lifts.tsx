import { TabPlaceholder } from '@/components/tab-placeholder';

/** Lifts (CLAUDE.md §11.2) — built in Phase 4. Empty-state copy from §12.1. */
export default function Lifts() {
  return <TabPlaceholder title="Lifts" note="Lifts appear here as you name them." />;
}

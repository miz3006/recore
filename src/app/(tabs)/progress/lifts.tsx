/**
 * THE LIFT LIBRARY, AS REACHED FROM PROGRESS — the other of two doors onto the
 * same screen (`components/lifts-screen.tsx`, which says why it is a component).
 *
 * The file exists so the push happens INSIDE Progress's stack: the tab bar
 * stays, the back control says "Progress", and the search field sits where it
 * sits on the tab this was pushed from.
 */
export { LiftsScreen as default } from '@/components/lifts-screen';
